Run:

```bash
./skills/setup.sh
```

This creates symlinks from this repository's local skills into:

```bash
$CODEX_HOME/skills
```

If `CODEX_HOME` is not set, it falls back to:

```bash
~/.codex/skills
```
